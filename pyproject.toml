[project]
name = "ietf-mail-mcp"
version = "0.1.0"
description = "An MCP server that fetches and reads emails from the IETF Mail Archive"
requires-python = ">=3.13"
dependencies = [
    "beautifulsoup4>=4.14.3",
    "httpx>=0.28.1",
    "mcp>=1.26.0",
]
